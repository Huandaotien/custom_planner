# Requirements:
- Ubuntu 20.04.6 LTS
- ROS Noetic

- Install sbpl library:
    ```
    git clone https://github.com/sbpl/sbpl.git
    mkdir build
    cd build
    cmake ..
    make -j4
    sudo make install
    ```
